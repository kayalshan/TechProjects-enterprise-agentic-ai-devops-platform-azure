replicaCount: 2

image:
  repository: tools-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8140

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

env:
  SPRING_PROFILES_ACTIVE: "prod"